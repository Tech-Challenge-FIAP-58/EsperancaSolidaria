apiVersion: v1
kind: Service
metadata:
  name: campaign-service
  namespace: services
spec:
  type: NodePort
  selector:
    app: campaignservice
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080